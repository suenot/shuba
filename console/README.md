# shuba console

A React single-page app that will provide a browser UI for the shuba orchestrator (sessions, events, config views to be added in later tasks). Build the static bundle with `bun run console:build` (outputs to `console/dist/main.js`, served alongside `console/index.html`), or run `bun run console:dev` to rebuild automatically on change while developing.
